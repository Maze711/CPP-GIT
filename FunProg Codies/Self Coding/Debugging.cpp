#include <iostream>

using namespace std;

int main(){
    int fnum, snum, tnum, sum;

    fnum = 10;
    snum = 20;
    tnum = 30;

    sum = fnum + snum + tnum;

    cout << "The sum of " <<fnum<<" "<<snum<<" "<<tnum<<" is: "<<sum<<endl;
}